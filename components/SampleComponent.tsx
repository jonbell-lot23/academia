const SampleComponent = () => {
  const calculation = 9 + 2;

  return (
    <div className="p-4 bg-blue-200 border border-blue-800 rounded-sm">
      The result of the calculation is: {calculation}
    </div>
  );
};

export default SampleComponent;
